# Data files — raw Excel workbooks (keep locally, do not commit)
*.xlsx
*.xls

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/
.env

# macOS
.DS_Store
.AppleDouble
.LSOverride
*.DS_Store
._*

# Jupyter
.ipynb_checkpoints/
*.ipynb

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Veryberry-field is its own repo (github.com/Aditya-k24/Veryberry-field),
# nested here on disk only for convenience. Never track it from this repo —
# `git add .` would otherwise commit it as a gitlink/submodule pointer instead
# of its actual files.
Veryberry-field/
